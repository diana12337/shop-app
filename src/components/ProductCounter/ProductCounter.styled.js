import styled from 'styled-components';

const StyledProductCounter = styled.div`
  border: 1px solid black;
  width: 100%;
  padding: 10px;

  display: flex;
  p {
    margin: 0;
  }
  button {
    border: none;
    background: transparent;
  }
`;

export default StyledProductCounter;
