import styled from 'styled-components';

const StyledUserData = styled.div`
  width: 50%;
  background: white;
  text-align: left;
  padding: 10px;

  h3 {
    text-align: center;
  }
  section {
    text-align: center;
  }
  label {
    font-size: 12px;
  }
`;

export default StyledUserData;
